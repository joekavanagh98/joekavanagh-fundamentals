# 05 — Express REST API

A small Express app with a layered architecture (routes →
controllers → services) and a `/books` resource. In-memory storage,
no database. Stub README, replaced when the implementation lands.
